{
  "enabled": true,
  "feature": "auto_typing",
  "model_dir": "",
  "use_subtree_stage2": true,
  "notes": "Auto-typing uses the v9 ML pipeline. The only runtime knobs are the optional model_dir override (point at custom-trained models) and use_subtree_stage2 (whether Stage 2 operates on full primary subtrees; default true)."
}
